<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="cn.lookup.sanye.mapper.MenuMapper">

    <!-- 通用查询映射结果 -->
    <resultMap id="BaseResultMap" type="cn.lookup.sanye.pojo.Menu">
        <id column="id" property="id" />
        <result column="name" property="name" />
        <result column="path" property="path" />
        <result column="component" property="component" />
        <result column="component_path" property="component_path" />
        <result column="icon" property="icon" />
        <result column="pid" property="pid" />
    </resultMap>
    <!-- 根据用户名查询菜单 -->
    <select id="findMenuByUserName" resultType="cn.lookup.sanye.pojo.Menu" parameterType="String">
		SELECT
        m.id,m.name,m.path,m.icon,m.pid,m.sort
        FROM sys_menu m
        INNER JOIN sys_role_menu rm ON m.id=rm.menu_id
        INNER JOIN sys_role r ON rm.role_id=r.id
        INNER JOIN sys_user_role ur ON ur.role_id=r.id
        INNER JOIN sys_user u ON ur.user_id=u.id
        WHERE u.username= #{userName}
        ORDER BY m.sort ASC
	</select>
    <!-- 通用查询结果列 -->
    <sql id="Base_Column_List">
        id, name, path, component, component_path, icon, pid
    </sql>

</mapper>
